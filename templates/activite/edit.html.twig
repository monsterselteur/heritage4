{% block stylesheets %}    <link rel="stylesheet" href="{{ asset('CSS/StyleNew.css') }}"> {% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Edit Activite{% endblock %}

{% block body %}
    <h1 class="text-center">Edit une nouvelle Activite</h1>
    <div class="formAll">
        <div class="button-container">
            {{ include('activite/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="{{ app.request.headers.get('referer') }}" class="return-btn">Retour</a>
            {{ include('activite/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
