<form method="post" action="{{ path('app_groupe_competence_delete', {'id': groupe_competence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe_competence.id) }}">
    <button class="btn">Delete</button>
</form>
