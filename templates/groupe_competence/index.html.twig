{% extends 'base.html.twig' %}

{% block title %}GroupeCompetence index{% endblock %}

{% block body %}
    <h1>GroupeCompetence index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for groupe_competence in groupe_competences %}
            <tr>
                <td>{{ groupe_competence.id }}</td>
                <td>{{ groupe_competence.nom }}</td>
                <td>
                    <a href="{{ path('app_groupe_competence_show', {'id': groupe_competence.id}) }}">show</a>
                    <a href="{{ path('app_groupe_competence_edit', {'id': groupe_competence.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_groupe_competence_new') }}">Create new</a>
{% endblock %}
