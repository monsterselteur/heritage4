<form method="post" action="{{ path('app_situation_delete', {'id': situation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ situation.id) }}">
    <button class="btn">Delete</button>
</form>
